# 荣耀真机测试记录

日期：2026-09-07。设备型号：JSN-AL00a，Android 10。

## 已实际验证

- 签名 Release APK 首次安装成功，主界面正常启动。
- 通知权限在本机默认开启；点击测试通知后，通知栏实际显示“到期闹钟 · 测试通知”及正文。
- 切换深色主题成功，结束应用进程并重新启动后主题保留。
- 输入 `Passport 2029-05-10`，识别事项、日期以及默认提前 30 天。
- 确认添加后结束应用进程并重新启动，列表记录仍存在。
- 编辑提前天数为 7，保存后列表显示提前 7 天。
- 删除本次创建的测试记录后，列表恢复为空。
- 测试结束停留在主页。
- 覆盖安装 `0.1.1`（版本码 2）成功，签名与上一测试版本兼容。
- 中文完整句子可识别：未写提前时间时采用设置中的 7 天；“提前3个月”识别为 90 天。
- 新增护照、健身会员和酸奶三类事项后，重新打开应用记录仍在。
- 缺少系统语音识别服务时显示键盘输入提示，应用不再闪退。
- 浅色、深色、默认提前天数、隐私说明和意见反馈入口均通过自动化真机检查。
- 正式提醒接收器经测试主动触发后，实际生成“当天到期”和“提前7天”两类系统通知。
- 系统已注册每日 09:00 的重复提醒任务；本机显示为非精确闹钟，系统允许延迟执行。

## 截图用途

根目录内 `01-home.png` 至 `05-edit.png` 是早期测试证据，手机当时使用较大字体，部分截图包含输入焦点或短暂提示。

随后按用户要求将系统字体缩放从 1.45 恢复为标准 1.0，并读取设置确认生效。显示密度仍为 540。`06-standard-font.png` 为标准字体下的截图。

`automated/` 内有标准字体下重新生成的 5 张真实手机截图，以及核心流程和提醒接收器的逐项报告。同一批截图已复制到 `release-materials/screenshots/`，可作为当前上架素材候选。

## 待验证

- 中文语音识别成功流程：本机没有可用的系统语音识别服务，需要换一台支持该服务的手机人工说话验证。
- 通知权限关闭后的界面表现：本机命令级切换没有改变系统返回状态，需要在系统设置中人工关闭后复测。
- 系统在每日检查时间自动唤醒、重启恢复和省电模式：不能用主动触发替代，需要跨时段留机观察。
- 卸载重装会清空本地记录，属于预期行为；尚未执行破坏性卸载测试。
- 小米/红米、OPPO/vivo 等其他厂商设备尚未测试。

本次接收器主动触发成功，不等于后台定时提醒可靠性验证完成。
